package mensa.api.hibernate.domain;

/**
 * Enumeration of all mensa lines, as can also be seen in the mensa's own API.
 * If this is reordered, the database will have to be adjusted accordingly.
 * @author Petar Vutov
 */
public enum Line {
	l1, l2, l3, l45, schnitzelbar, update, abend, aktion, heisstheke, nmtisch
}
