package mensa.api.hibernate.domain;

import javax.persistence.Entity;
import javax.persistence.Id;

// Re-ordering WILL mess up the database
public enum Tag {
	VEGETARISCH, MIT_URANIUM, EXTRA_GESUND, PFERDESCHWANZ
}
